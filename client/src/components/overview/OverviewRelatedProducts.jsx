import React from 'react';

const RelatedProducts = (props) => {
  // Should link to the related products and outfits portion (currently doesn't exist)
  // Add a title above to indicate link to related products
  // Maybe have fun with this and link to a Blade Runner page of some kind?

  // console.log('relatedProducts page: ', props);
  // props gives an array of product ids that are similar to the listed one

  return <button>+</button>
};

export default RelatedProducts;
