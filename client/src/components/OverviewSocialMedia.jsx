import React from 'react';

const Socials = (props) => {
  // Add three buttons: Facebook, Twitter, Pinterest
  // Use standard icons for each company
  // Link to the website, have some fun with this like linking to Blade Runner stuff
  return <button>Facebook</button>
};

export default Socials;
